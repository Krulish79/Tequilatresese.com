# macOS metadata
.DS_Store

# Adobe Illustrator source files (not needed for the website)
*.ai

# Claude Code session data (local only)
.claude/

# Editor / OS junk
*.swp
*.swo
Thumbs.db

# Node / build artifacts (just in case)
node_modules/
dist/
build/
videos/bottling-02.orig.mp4
.tools/
images/slides/cultivation/_orig/

# PPTX with embedded videos exceeds GitHub 100 MB file limit — kept locally only.
# The PPTX is regenerable from the SVGs + videos using the build script.
Brand Materials/Presentacion ES · PPTX/*.pptx

# Editor swap files (e.g. .!12881!filename created by some editors)
.!*!*
